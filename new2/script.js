let scrollImage = document.querySelector('.scroll-image');

function scrollLeft() {
    scrollImage.scrollLeft -= 350; // adjust the value to scroll by
}

function scrollRight() {
    scrollImage.scrollLeft += 350; // adjust the value to scroll by
}